import cn from "classnames/bind";
import pageStyles from "../pageStyles/Home.module.scss";

const cx = cn.bind(pageStyles);

export default function Home() {
  return <h1>Welcome to ExploraNotes!</h1>;
}
